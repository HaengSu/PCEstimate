<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PCEstimate"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity
            android:name=".view.estimate.ActivityPriceSelector"
            android:exported="true"/>

        <activity android:name=".view.estimate.ActivityEstimateResult"
            android:exported="true"/>

        <activity android:name=".view.board.ActivityMainBoard"
            android:exported="true"/>

        <activity android:name=".view.board.ActivityWritePost"
            android:exported="true"/>
        <activity android:name=".view.board.ActivityPostSearch"
            android:exported="true"/>
        <activity android:name=".view.board.ActivityDetailPost"
            android:exported="true"/>


    </application>

</manifest>